<Window x:Class="Markup.Programming.Samples.OneMethodTwoBindings.View"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:p="http://programmability.codeplex.com/markup/programming"
    xmlns:local="clr-namespace:Markup.Programming.Samples.OneMethodTwoBindings"
    Title="OneMethodTwoBindings" Height="300" Width="300">
    <Window.Resources>
        <local:ViewModel x:Key="ViewModel"/>
    </Window.Resources>
    <Window.DataContext>
        <Binding Source="{StaticResource ViewModel}"/>
    </Window.DataContext>
    <Grid>
        <StackPanel>
            <Button Content="Match Boy with Girl">
                <i:Interaction.Triggers>
                    <p:CallHandler Path="Click!Match">
                        <p:Get Source="{Binding SelectedValue, ElementName=Boys}"/>
                        <p:Get Source="{Binding SelectedValue, ElementName=Girls}"/>
                    </p:CallHandler>
                </i:Interaction.Triggers>
            </Button>
            <UniformGrid Columns="2">
                <ListBox Name="Boys" ItemsSource="{Binding Boys}"/>
                <ListBox Name="Girls" ItemsSource="{Binding Girls}"/>
            </UniformGrid>
        </StackPanel>
    </Grid>
</Window>
